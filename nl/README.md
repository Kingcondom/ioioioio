# เกณฑ์ความรู้ความสามารถฯ แพทยสภา พ.ศ. 2567 — ฉบับแกะเป็น JSON

ต้นฉบับ: ประกาศแพทยสภาที่ 4/2567 · 106 หน้า · **ไม่มี text layer**
(Google Drive id `143DRB8wSwIfDvji6OfC--FRIUitv7kbm`)

| ไฟล์ | เนื้อหา |
|---|---|
| `nl_partA.json` | ภาค ก. วิทยาศาสตร์การแพทย์พื้นฐาน B1–B11 · **698 รหัส** (หน้า 6–47) |
| `nl_partB.json` | ภาค ข. ความรู้ความสามารถทางวิชาชีพฯ · **511 รหัส** (หน้า 56–74) |
| `visA/`, `vis/` | ผลอ่านดิบทีละหน้า (`visA/_scheme.md` = คำอธิบายโครงรหัส) |
| `build_visA.py`, `build_vis.py` | `vis*/` → `nl_part*.json` |
| `merge_full.py` | รวมทั้งสองภาค + หัวข้อระดับหมวด → `artifact/data/nl.json` (1,389 รหัส) |

## วิธีที่ใช้แกะ

เรนเดอร์หน้า PDF เป็น PNG 200 dpi แล้ว **อ่านด้วยสายตา** (vision) ทีละหน้า
→ แม่นยำ 100% · ต้นทุน ~3.8k tokens/หน้า

> ⚠️ **อย่าลองใช้ tesseract OCR ซ้ำ** — ลองมาแล้ว 3 วิธี (ทั้งหน้า / crop แยกคอลัมน์ /
> TSV พิกัดคำ) ได้ดีที่สุด 71% แย่ที่สุด 37% และหมวด 2.1/2.2/3.x หายทั้งหมด ใช้ไม่ได้

## โครงรหัส

- **ภาค ก.** `BX.1` โครงสร้าง/หน้าที่ · `BX.2.1`–`.2.6` สาเหตุแยกตามกลไก
  (hereditary / infectious / traumatic / neoplastic / metabolic / vascular) ·
  `BX.3` การสืบค้น · `BX.4` ยาสมเหตุผล
- **ภาค ข.** `2.1.x` อาการสำคัญ (84) · `2.2.x` ภาวะฉุกเฉิน (50) ·
  `2.3.<ระบบ>(n)` โรคตามระบบ · `3.1`–`3.3` การตรวจ (52)
- ตารางทุกตัวแบ่ง **ซ้าย = กลุ่มที่ 1 และ 2** (ดูแลเองได้) /
  **ขวา = กลุ่มที่ 3** (วินิจฉัยแล้วส่งต่อ) → รหัสกลุ่ม 3 มี `-3` คั่น เช่น `2.3.9-3(3)`

## ข้อผิดพลาดในตัวเอกสารที่พบ (บันทึกไว้ในฟิลด์ title ของรหัสนั้น)

- `B1.6.4` ใช้ซ้ำสองหัวข้อ → เก็บอันหลังเป็น `B1.6.5`
- `B5.2.2` กลุ่ม 3 นับ (1)–(5) จบหน้า 27 แล้วหน้า 28 **เริ่มนับใหม่ที่ (2)**
  (ตรวจซ้ำด้วย crop 400 dpi แล้ว) → เรียงต่อเป็น (6)(7)(8)
- `B11.2.6` พิมพ์ขาด B · หัวข้อ 2.2 เขียน "49 รายการ" แต่เลขไล่ถึง 2.2.50
