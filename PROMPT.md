# พรอมป์สำหรับแชทถัดไป

อัปโหลด `med421-kit.zip` เข้าแชทใหม่ แล้ววางข้อความด้านล่างนี้ (แก้ชื่อระบบที่ต้องการ)

---

## A. ทำระบบใหม่ต่อ (ใช้บ่อยสุด)

```
แตกไฟล์ med421-kit.zip แล้วอ่าน SPEC.md ก่อนเริ่ม

งานวันนี้: ทำชุด "Infectious disease" ต่อจาก Cardio / Nephro / Chest ที่เสร็จแล้ว
ทำตามมาตรฐานใน SPEC.md ทุกข้อ ห้ามลดสเปก

1. อ่าน MED421-422_Lecture_List.txt ใน project หาว่า lecture ของระบบนี้คือคาบไหนบ้าง
   (เลขคาบ วันที่ ชื่ออาจารย์ และเป็นคาบ AL หรือไม่)
2. สกัดข้อสอบเก่าของระบบนี้จาก "โพยใหญ่ MED.pdf" (MED28-MED34) และ "MED35.pdf"
   แล้วเรียบเรียงเป็น MCQ 5 ตัวเลือกพร้อมเฉลย+กลไก ตาม schema §3.3 ของ SPEC.md
   -> data/id_old_a.json
3. สร้างข้อสอบใหม่ 50 ข้อ (MCQ 40 + MEQ 5 + OSCE/SAQ 5) ตาม schema §3.1 และ §3.2
   -> data/id_mcq_a.json, data/id_mcq_b.json, data/id_meq_osce.json
4. เพิ่ม block ของระบบนี้ใน config.json (พร้อมสีและ lecture_order) และเพิ่ม lecture ใหม่ใน config.lectures
4.1 ผูกรหัสเกณฑ์แพทยสภา พ.ศ. 2567 ในฟิลด์ "nl" ของทุกข้อ (ทั้งข้อใหม่และคลังเก่า)
    - ใช้รหัสจาก refs/nl_2567.json ถ้ารหัสที่ต้องใช้ยังไม่มี ให้เพิ่มเข้าไปพร้อม title/section/page/group ตามต้นฉบับประกาศ
    - build.py จะเตือนให้เองว่าข้อไหนยังไม่ผูก และรหัสไหนไม่มีในดัชนี (ดู SPEC.md §7)
5. รัน python3 check_numbers.py แล้วแก้จนได้ "ตัวเลขไม่สอดคล้อง 0 ข้อ"
6. ตรวจทานทางการแพทย์อีกรอบ: key ถูกตามแนวทางปัจจุบันไหม, มีตัวลวงที่ถูกด้วยไหม,
   ตัวเลขในโจทย์ขัดกับคำอธิบายไหม, โพยตอบผิดตรงไหนบ้าง
7. รัน python3 build.py แล้วส่ง drill.html + MED421_<ระบบ>.pdf กลับมา

ข้อควรรู้: เขียน "answer": 0 เสมอ และวางคำตอบที่ถูกไว้ choices[0] — build.py สลับตำแหน่งให้เอง
```

---

## B. แปลงคลังข้อสอบเก่า Cardio/Nephro ให้เป็นตัวเลือก

```
แตกไฟล์ med421-kit.zip แล้วอ่าน SPEC.md

data/cardio_old.json (130 ข้อ) และ data/nephro_old.json (124 ข้อ)
ยังเป็นรูปแบบเก่า {part, q, a, src} อยู่
ช่วยแปลงให้เป็นรูปแบบตัวเลือก 5 ข้อพร้อมเฉลย ตาม schema §3.3 ของ SPEC.md
(เหมือนที่ทำกับ data/chest_old_*.json แล้ว — ดูไฟล์นั้นเป็นตัวอย่าง)

ข้อสำคัญ
- ตัวลวงต้องเป็นสิ่งที่คนพลาดจริง ไม่ใช่ตัวเลือกที่ผิดชัดจนไม่มีใครเลือก
- ถ้าคำเฉลยเดิมในโพยไม่ตรงกับแนวทางปัจจุบัน ให้เขียนบอกไว้ใน note อย่าลอกตาม
- เก็บข้อความต้นฉบับที่โพยจดไว้ในฟิลด์ src เสมอ
- ใส่ฟิลด์ nl (รหัสเกณฑ์แพทยสภา 2567) ให้ทุกข้อด้วย — ดู data/air_old_a.json เป็นตัวอย่าง
- ไล่ตัวเลข ABG/Light's ให้สอดคล้องกัน (รัน check_numbers.py)
ทำเสร็จแล้วรัน build.py แล้วส่งไฟล์กลับมา
```

---

## C. ติวสด จากคลังที่มีอยู่

```
แตกไฟล์ med421-kit.zip
อ่าน bank_merged.json แล้วสุ่มข้อจากชุด <ระบบ> lecture <เลขคาบ> มาถามทีละข้อ
ห้ามเฉลยจนกว่าจะตอบ แล้วค่อยอธิบาย diagnostic reasoning และการตัดช้อยส์
```

---

## สิ่งที่ควรแนบเข้าแชทใหม่ด้วย

- `med421-kit.zip` (อันนี้)
- `โพยใหญ่ MED.pdf` และ `MED35.pdf` — ถ้าจะให้สกัดข้อสอบเก่าของระบบใหม่
- project doc `MED421-422_Lecture_List.txt` มีอยู่ใน project อยู่แล้ว ไม่ต้องแนบ
