# PROMPT.md — พรอมป์สำเร็จรูป copy ไปวางในแชทใหม่ได้เลย

ทุกแบบ: ลากโฟลเดอร์/zip ของ kit เข้าไปในแชทก่อน แล้ววางข้อความด้านล่าง

---

## แบบที่ 1 — ทำระบบใหม่ทั้งระบบ

```
นี่คือ med421-kit อ่าน SPEC.md กับ config.json ก่อน
ทำระบบใหม่: <ชื่อระบบ เช่น GI — Gastrointestinal system>
1. เพิ่ม entry ใหม่ใน config.json (key, ชื่อไทย/อังกฤษ, สี, seed ที่ยังไม่ถูกใช้, ปฏิทิน lecture ตามนี้: <วางรายชื่อ lecture>)
2. เขียน data/<key>.json ตาม schema ใน SPEC.md — MCQ <จำนวน> ข้อ + MEQ <จำนวน> ข้อ
   - กระจายให้ครบทุก lecture
   - ทุกข้อต้องมี ref, pearl และผูก nl จาก refs/nl_2567.json (ถ้ารหัสที่ต้องใช้ยังไม่มีในดัชนี ให้เพิ่มเข้าไปพร้อมชื่อเต็ม ส่วน/หน้า)
   - เขียน answer: 0 เสมอ วางคำตอบถูกไว้ตัวแรก
3. รัน python3 check_numbers.py และ python3 build.py <key>
4. รายงานว่าแต่ละ lecture มีกี่ข้อ และครอบคลุมหัวข้อ NL อะไรบ้าง / ยังขาดหัวข้อไหน
```

## แบบที่ 2 — แปลงคลังเก่า (โพย/ข้อสอบเดิม) เข้าระบบ

```
นี่คือ med421-kit อ่าน SPEC.md ก่อน
ไฟล์ที่แนบคือโพยข้อสอบเดิม <ชื่อไฟล์>
แปลงเข้า data/<key>.json ตาม schema:
- id ใช้รูปแบบ <SYS>-OLD-<nn> สำหรับข้อที่ยกมาจากโพย
- เก็บเฉลยเดิมไว้เป็น choices[0] เสมอ
- เติม pearl และ ref ให้ครบทุกข้อ (ถ้าโพยไม่มีคำอธิบาย ให้เขียนใหม่จาก guideline ปัจจุบันและระบุปีใน ref)
- ถ้าตัวเลขในโจทย์ขัดกันเอง (ABG/electrolyte) ให้แก้ให้สอดคล้อง แล้วบันทึกไว้ใน source_note ว่าแก้อะไรจากต้นฉบับ
- ผูก nl ทุกข้อ
จบแล้วรัน check_numbers.py + build.py แล้วสรุปว่าแก้ตัวเลขข้อไหนบ้าง
```

## แบบที่ 3 — ติวสด (ไม่ต้อง build)

```
นี่คือ med421-kit ใช้ data/<key>.json เป็นคลัง
ติวฉันแบบ active recall:
- สุ่มทีละข้อจาก lecture <ระบุ หรือ "ทุก lecture">
- ถามโจทย์ + ตัวเลือกก่อน อย่าเพิ่งเฉลย
- ฉันตอบแล้วค่อยเฉลย พร้อมบอกว่าทำไมตัวลวงแต่ละตัวผิด แล้วอ่าน pearl ให้ฟัง
- ทุก 10 ข้อ สรุปว่าฉันพลาดหัวข้อ NL ไหนซ้ำ ๆ และควรกลับไปอ่านอะไร
```

---

### คำสั่งที่ใช้บ่อย

```bash
python3 build.py                 # ทุกระบบ
python3 build.py air --no-pdf    # ระบบเดียว ไม่ทำ PDF (เร็ว ใช้ตอนเขียนข้อ)
python3 check_numbers.py         # ตรวจตัวเลข ABG ทั้งคลัง
python3 check_numbers.py air     # ตรวจเฉพาะระบบ
```
